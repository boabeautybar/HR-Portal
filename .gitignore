node_modules/
.DS_Store
.venv/

# Local secrets — Netlify Function env vars (Google Maps key, etc.). Never commit.
# .env.example IS committed as a template; the real .env is not.
.env
.env.local
.env.*.local

# Fresha exports — contain client PII; imported into Supabase, never committed
*.csv
Gift Card Transactions.csv

# Local Netlify folder
.netlify

# Editor / AI-tooling local config — not part of the app
.antigravity/
.repowise/
.vscode/
.mcp.json
# .claude/CLAUDE.md IS committed (it's the repo's instructions); the
# settings.local.json beside it is per-machine tool permissions and is not.
.claude/settings.local.json
